package com.ssafy.develog.interview.domain;

import org.springframework.data.jpa.repository.JpaRepository;

public interface RecordDetailKeywordRepository extends JpaRepository<RecordDetailKeyword, Long> {
}
